# MDGFM graph-level few-shot finetune — scripts/mdgfm/finetune_graph.py
# Usage: python scripts/mdgfm/finetune_graph.py --ckpt PATH (driver has no -c; reference only)

dataset: Cora
k_shot: 1
ckpt: null
downstream_root: downstream_data/mdgfm
splits_path: null
split_id: 0
task_num: 0
data_root: datasets/mdgfm
seed: 42
lr: 0.001
patience: 50
max_steps: 400
test_reserve: 1000
max_one_hop: 10
max_two_hop: 4
